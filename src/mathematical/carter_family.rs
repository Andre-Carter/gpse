use std::f64::consts::PI;
//sequencial mathematics vs individal math """"
const carter_ratio = PI / 4.0;
const carter_constant = 1.0 - carter_ratio;

pub fn carter_family_function(n: f64) -> f64 {
    let carter_ratio = PI / 4.0;
    let carter_constant = 1.0 - (PI / 4.0);
}

//impl
